package co.edu.unipiloto.myapplication.rest;

public class Post {
    private int userId; // Matches "userId" in the JSON [cite: 265]
    private int id;     // Matches "id" in the JSON [cite: 266]
    private String title; // Matches "title" in the JSON [cite: 267]
    private String body;  // Matches "body" in the JSON [cite: 268]

    // Include getters and setters for all fields (as shown in the document [cite: 316, 357]).
}